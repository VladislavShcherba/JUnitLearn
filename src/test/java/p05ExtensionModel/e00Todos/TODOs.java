package p05ExtensionModel.e00Todos;

/**
 * Registering Extensions.
 * Register extension with @ExtendWith at the class level, method level, or parameter level.
 * Take a look at which order extensions are registered. Register on fields. Take a look at the ordering
 * with and without @Order.
 * Register extension with @RegisterExtension. Take a look at ordering. Static and instance fields. Ordering.
 * Is it only for fields? Which extensions can be registered, which cannot.
 * Take a look at inheritance of extensions, including from class to method.
 */
class TODOs {}
